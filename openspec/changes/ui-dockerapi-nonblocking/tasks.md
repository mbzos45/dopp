## 1. 仕様反映の準備

- [x] 1.1 `specs/async-docker-ops/spec.md` の要求事項を実装観点で整理する
- [x] 1.2 既存のUI/Docker呼び出し箇所を洗い出す（`src/ui.rs`, `src/docker.rs`）

## 2. 非同期タスク実行の基盤

- [x] 2.1 `UiEvent` と送受信チャンネルの型定義を追加する
- [x] 2.2 Tokioランタイムとタスク起動を担うランナーを用意する
- [x] 2.3 `request_repaint()` を安全に呼ぶ経路を定義する

## 3. Docker操作の非同期化

- [x] 3.1 Docker一覧取得を非同期タスク化し、結果を `UiEvent` で返す
- [x] 3.2 start/stop/restart を非同期タスク化し、結果を `UiEvent` で返す
- [x] 3.3 成功/失敗に関わらず一覧更新タスクを起動する

## 4. UI状態管理と描画更新

- [x] 4.1 UI側で `UiEvent` を毎フレームdrainする処理を追加する
- [x] 4.2 コンテナ単位のロード中フラグを管理する
- [x] 4.3 エラー表示を `UiEvent` 経由の更新に切り替える

## 5. 動作確認

- [ ] 5.1 UI操作がブロックせず反応し続けることを確認する
- [ ] 5.2 start/stop/restart後に必ず一覧が更新されることを確認する
